export default function AboutLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  // currently doing nothing
  // Can be used to add layout to this and nested pages
  return <>{children}</>;
}
